// Declares functions so TypeScript doesn't murder me.
// These come from OverlayPlugin, and are linked in index.html.
declare function addOverlayListener(eventType: string, callback: Function);
declare function startOverlayEvents();